% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-internal.R
\name{aux_integrals}
\alias{aux_integrals}
\title{Compute auxiliary integrals for M2PL Delta-method SEs (D-dimensional)}
\usage{
aux_integrals(a, d, theta_mean = NULL, theta_cov = NULL, n_pts = NULL)
}
\arguments{
\item{a}{Numeric vector of length D: discrimination parameters.}

\item{d}{Scalar intercept parameter.}

\item{theta_mean}{Latent mean vector (length D, default zeros).}

\item{theta_cov}{Latent covariance matrix (D x D, default identity).}

\item{n_pts}{Quadrature points per dimension (auto-selected if \code{NULL}).}
}
\value{
Named list:
\itemize{
\item \code{gamma}      — scalar  integral of P * Q * f
\item \code{mu_vec}     — D-vector of integrals of P * Q * theta_k * f
\item \code{lambda_vec} — D-vector of integrals of P * Q * (1 - 2P) * theta_k * f
\item \code{kappa}      — scalar  integral of P * Q * (1 - 2P) * f
}
}
\description{
Evaluates the four vector/scalar integrals (gamma, mu_vec, lambda_vec,
kappa) needed for the gradients of B_j, A_j (vector), and A_j (scalar)
w.r.t. item parameters \verb{(a, d)} under a D-variate normal population.
}
\details{
For D = 1 a 100-point 1-D quadrature is used; for D >= 2 a product grid
of 40 points per dimension is used.
}
\keyword{internal}
