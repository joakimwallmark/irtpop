% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_metrics.R
\name{item_metrics}
\alias{item_metrics}
\title{Population item metrics for a fitted mirt model}
\usage{
item_metrics(
  mod,
  items = NULL,
  max_score = NULL,
  theta_mean = NULL,
  theta_cov = NULL,
  se = FALSE
)
}
\arguments{
\item{mod}{A fitted object of class \code{"SingleGroupClass"} or
\code{"MultipleGroupClass"} as returned by \code{\link[mirt:mirt]{mirt::mirt()}}.}

\item{items}{Integer vector of item indices to evaluate. Defaults to all
items.}

\item{max_score}{Named integer vector or single integer giving the maximum
possible score for each item. If a single value is supplied it is recycled.
Defaults to \code{item@ncat - 1} for each item.}

\item{theta_mean}{Numeric vector overriding the latent mean. Defaults to the
model's estimated group means.}

\item{theta_cov}{Numeric matrix overriding the latent covariance. Defaults
to the model's estimated group covariance.}

\item{se}{Logical. If \code{TRUE}, Delta-method standard errors are
appended for each metric. SEs are only supported for 2PL items
(\code{itemtype = "2PL"} or \code{"Rasch"}); other item types produce \code{NA} SE
columns. Requires the model to have been fitted with \code{SE = TRUE}.}
}
\value{
A \code{data.frame} with columns:
\itemize{
\item \code{item} — item index (integer)
\item \code{item_name} — item name from the model
\item \code{B} — population difficulty
\item \code{A} — scalar population discrimination
\item \verb{A_1, A_2, ...} — vector population discrimination components (one per
latent dimension)
}

When \code{se = TRUE}, additional columns \code{B_se}, \code{A_se}, \code{A_1_se}, \code{A_2_se},
... are appended.
}
\description{
A convenience wrapper that computes \code{\link[=population_difficulty]{population_difficulty()}} and
\code{\link[=population_discrimination]{population_discrimination()}} for every item (or a subset) in a fitted
\code{\link[mirt:mirt]{mirt::mirt()}} model and returns the results as a \code{data.frame}.
}
\details{
The latent distribution parameters (\code{theta_mean}, \code{theta_cov}) are taken
from the model's estimated group parameters unless overridden by the caller.
}
\examples{
\dontrun{
library(mirt)
mod <- mirt(Science, 1, itemtype = "graded", verbose = FALSE)
item_metrics(mod)

# With Delta-method SEs (requires SE = TRUE during fitting)
data <- expand.table(LSAT7)
mod_se <- mirt(data, 1, itemtype = "2PL", verbose = FALSE, SE = TRUE)
item_metrics(mod_se, se = TRUE)
}

}
\seealso{
\code{\link[=population_difficulty]{population_difficulty()}}, \code{\link[=population_discrimination]{population_discrimination()}},
\code{\link[=se_pop_difficulty]{se_pop_difficulty()}}, \code{\link[=se_pop_discrimination_scalar]{se_pop_discrimination_scalar()}},
\code{\link[=se_pop_discrimination_vector]{se_pop_discrimination_vector()}}
}
