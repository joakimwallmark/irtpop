% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/se_delta_method.R
\name{se_pop_difficulty}
\alias{se_pop_difficulty}
\title{Delta-method SE for population difficulty (M2PL, any D)}
\usage{
se_pop_difficulty(a, d, vcov_j, theta_mean = NULL, theta_cov = NULL)
}
\arguments{
\item{a}{Numeric discrimination vector of length D.}

\item{d}{Scalar intercept parameter.}

\item{vcov_j}{\eqn{(D+1) \times (D+1)} covariance matrix for
\eqn{(a_1, \ldots, a_D, d)}.}

\item{theta_mean}{Latent mean vector (length D). Defaults to zeros.}

\item{theta_cov}{Latent covariance matrix (D x D). Defaults to identity.}
}
\value{
A non-negative scalar standard error.
}
\description{
Computes the standard error of the population difficulty estimator \eqn{B_j}
via the multivariate Delta method. The gradient with respect to the item
parameter vector \eqn{\boldsymbol{\xi}_j = (a_1, \ldots, a_D, d)} is
\deqn{\nabla_{\boldsymbol{\xi}_j} B_j = (-\boldsymbol{\mu}, -\gamma)}
where \eqn{\gamma = \int P Q f\, d\boldsymbol{\theta}} and
\eqn{\boldsymbol{\mu}} is the D-vector
\eqn{\int P Q\, \boldsymbol{\theta}\, f\, d\boldsymbol{\theta}}.
}
\seealso{
\code{\link[=se_pop_discrimination_scalar]{se_pop_discrimination_scalar()}}, \code{\link[=se_pop_discrimination_vector]{se_pop_discrimination_vector()}},
\code{\link[=item_metrics]{item_metrics()}}
}
